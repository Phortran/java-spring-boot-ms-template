description: Data object to update an existing recipe
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        toUpdate:
          $ref: ../schemas/OptionalRecipeBody.yaml
        toDelete:
          $ref: ../schemas/OptionalRecipeBody.yaml
